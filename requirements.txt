# Avelyn — Python Dependencies
# Install with: pip install -r requirements.txt

# ── UI framework ───────────────────────────────────────────────────────────────
PyQt6>=6.6.0

# ── Global hotkey detection (cross-platform) ───────────────────────────────────
pynput>=1.7.6

# ── Clipboard read / write ────────────────────────────────────────────────────
pyperclip>=1.8.2

# ── Keyboard / mouse simulation (copy + paste) ────────────────────────────────
pyautogui>=0.9.54

# ── Image processing (icon generation, pyautogui dependency on macOS) ─────────
Pillow>=10.0.0

# ── AI backends ───────────────────────────────────────────────────────────────
requests>=2.31.0                # Ollama local REST API

# ── Utilities ─────────────────────────────────────────────────────────────────
packaging>=23.0                 # Version comparisons

# ── Windows-only: process/window management for focus restoration ──────────────
# These are imported conditionally (only on Windows) but must be installed.
# On macOS/Linux: pip will install them but they will never be imported.
pywin32>=306; sys_platform == "win32"
psutil>=5.9.0
